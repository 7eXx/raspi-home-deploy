# broker information
BROKER_HOST=mosquitto
BROKER_PORT=9001
# client identification
CLIENT_ID=paho_backend
# publish timout to topic
PUBLISH_TIMEOUT=3
# the topic to publish status information
STATUS_TOPIC=mqtt/status
# command topic to change the state
COMMAND_TOPIC=mqtt/command
# Logging Level
LOGGING_LEVEL=INFO

LOG_FILE="{{ log_file }}"

# # raspi test new
BOT_TOKEN="{{ bot_token }}"
BOT_NAME="{{ bot_name }}"

# # Tex id
CHAT_IDS="{{ chat_ids }}"

JENKINS_USER="{{ jenkins_user }}"
JENKINS_PASS="{{ jenkins_pass }}"

WAKE_LAN_JOB_URL={{ wake_lan_job_url }}
WAKE_LAN_TOKEN={{ wake_lan_token }}

RYZEN_MAC_ADDR="{{ ryzen_mac_addr }}"
LUIGI_LENOVO_MAC_ADDR="{{ luigi_lenovo_mac_addr }}"